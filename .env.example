# Vercel KV (Redis) credentials
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Base URL for the application when running in Edge Runtime
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Debug logging - set to 'true' to enable verbose logging
DEBUG_LOGGING=false